---
import { findScheduleAttribute } from "../../tools/dataGetter";
import getElement from "../../tools/getElement";
import { default as ReactLiveSchedule } from "./LiveSchedule";

interface Props {
  code: string;
}

const { code } = Astro.props;
---

<ReactLiveSchedule code={code} />
